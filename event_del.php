<?php
	
	// $_POST['id'] contains the id of event to be deleted.
	function del_event($id) {
		// query sql database to make sure id is valid, escape as necessary, delete item, 
		// and redirect to landing page which will list remaining events.
	}